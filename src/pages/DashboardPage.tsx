import { AlertTriangle, CalendarClock, CircleDollarSign, Gauge, TrendingUp, Zap, } from "lucide-react"; import "./DashboardStep1.css"; type DashboardRow = { id: number; name: string; icon: string; kwh: number; watts: number; quantity: number; hoursPerDay: number; daysPerMonth: number; warrantyExpiryDate: string; nextServiceDate: string; }; type DashboardPageProps = { rows: DashboardRow[]; totalKwh: number; energyCharge: number; ftCharge: number; serviceCharge: number; vat: number; totalBill: number; vatRate: number; }; const money = (value: number) => value.toLocaleString("th-TH", { minimumFractionDigits: 2, maximumFractionDigits: 2, }); const dateStatus = (dateString: string) => { if (!dateString) return null; const date = new Date(`${dateString}T23:59:59`); if (Number.isNaN(date.getTime())) return null; return Math.ceil((date.getTime() - Date.now()) / 86400000); }; function DashboardPage({ rows, totalKwh, energyCharge, ftCharge, serviceCharge, vat, totalBill, vatRate, }: DashboardPageProps) { const averageKwhPerDay = totalKwh / 30; const averageBillPerDay = totalBill / 30; const topAppliances = [...rows] .sort((a, b) => b.kwh - a.kwh) .slice(0, 5); const top = topAppliances[0]; const topShare = totalKwh > 0 && top ? (top.kwh / totalKwh) * 100 : 0; const alerts = rows.flatMap((item) => { const result: { type: "energy" | "service" | "warranty"; title: string; text: string; }[] = []; if (totalKwh > 0 && item.kwh / totalKwh >= 0.3) { result.push({ type: "energy", title: `${item.icon} ${item.name} ใช้ไฟฟ้าสูง`, text: `ใช้ไฟฟ้า ${item.kwh.toFixed( 1 )} kWh/เดือน คิดเป็น ${( (item.kwh / totalKwh) * 100 ).toFixed(1)}% ของการใช้ไฟฟ้าทั้งหมด`, }); } const serviceDays = dateStatus(item.nextServiceDate); if (serviceDays !== null && serviceDays <= 30) { result.push({ type: "service", title: `🔧 ${item.name} ใกล้ถึงกำหนดบำรุงรักษา`, text: serviceDays < 0 ? "เลยกำหนดบำรุงรักษาแล้ว" : `เหลืออีก ${serviceDays} วัน`, }); } const warrantyDays = dateStatus(item.warrantyExpiryDate); if ( warrantyDays !== null && warrantyDays >= 0 && warrantyDays <= 30 ) { result.push({ type: "warranty", title: `🛡️ ${item.name} ใกล้หมดประกัน`, text: `เหลือระยะเวลาประกันอีก ${warrantyDays} วัน`, }); } return result; }); return ( <div className="dashboard-step1"> <section className="dashboard-hero"> <div> <div className="dashboard-eyebrow"> <Gauge size={18} /> Dashboard </div> <h1>ภาพรวมการใช้ไฟฟ้า</h1> <p> สรุปการใช้พลังงาน ค่าไฟฟ้า และอุปกรณ์ไฟฟ้าภายในบ้าน </p> </div> <div className="dashboard-hero-icon"> <Zap size={42} /> </div> </section> <section className="dashboard-stats"> <div className="dashboard-stat-card"> <div className="dashboard-stat-icon"> <Zap size={22} /> </div> <div> <span>การใช้ไฟฟ้ารวม</span> <strong>{totalKwh.toFixed(1)} kWh</strong> <small> เฉลี่ย {averageKwhPerDay.toFixed(2)} kWh/วัน </small> </div> </div> <div className="dashboard-stat-card"> <div className="dashboard-stat-icon"> <CircleDollarSign size={22} /> </div> <div> <span>ค่าไฟโดยประมาณ</span> <strong>{money(totalBill)} บาท</strong> <small> เฉลี่ย {money(averageBillPerDay)} บาท/วัน </small> </div> </div> <div className="dashboard-stat-card"> <div className="dashboard-stat-icon"> <TrendingUp size={22} /> </div> <div> <span>อุปกรณ์ที่ใช้ไฟสูงสุด</span> <strong>{top ? top.name : "-"}</strong> <small> {top ? `${topShare.toFixed(1)}% ของการใช้ไฟทั้งหมด` : "-"} </small> </div> </div> <div className="dashboard-stat-card"> <div className="dashboard-stat-icon"> <AlertTriangle size={22} /> </div> <div> <span>รายการแจ้งเตือน</span> <strong>{alerts.length} รายการ</strong> <small>ควรตรวจสอบรายการที่มีความเสี่ยง</small> </div> </div> </section> <section className="dashboard-grid"> <div className="dashboard-panel"> <div className="dashboard-panel-header"> <div> <h2>อุปกรณ์ที่ใช้ไฟฟ้าสูงสุด</h2> <p>เรียงตามปริมาณการใช้ไฟฟ้าต่อเดือน</p> </div> <TrendingUp size={22} /> </div> <div className="dashboard-ranking"> {topAppliances.length === 0 ? ( <div className="dashboard-empty"> ยังไม่มีข้อมูลอุปกรณ์ไฟฟ้า </div> ) : ( topAppliances.map((item, index) => { const percentage = totalKwh > 0 ? (item.kwh / totalKwh) * 100 : 0; return ( <div className="dashboard-ranking-row" key={item.id}> <div className="dashboard-rank"> #{index + 1} </div> <div className="dashboard-device-icon"> {item.icon} </div> <div className="dashboard-device-info"> <strong>{item.name}</strong> <span> {item.watts} W × {item.quantity} เครื่อง ×{" "} {item.hoursPerDay} ชม./วัน </span> <div className="dashboard-progress"> <div className="dashboard-progress-bar" style={{ width: `${Math.min(percentage, 100)}%` }} /> </div> </div> <div className="dashboard-device-value"> <strong>{item.kwh.toFixed(1)} kWh</strong> <span>{percentage.toFixed(1)}%</span> </div> </div> ); }) )} </div> </div> <div className="dashboard-panel"> <div className="dashboard-panel-header"> <div> <h2>สรุปค่าไฟฟ้า</h2> <p>รายละเอียดค่าใช้จ่ายโดยประมาณ</p> </div> <CircleDollarSign size={22} /> </div> <div className="dashboard-cost-list"> <div> <span>ค่าพลังงานไฟฟ้า</span> <strong>{money(energyCharge)} บาท</strong> </div> <div> <span>ค่า Ft</span> <strong>{money(ftCharge)} บาท</strong> </div> <div> <span>ค่าบริการ</span> <strong>{money(serviceCharge)} บาท</strong> </div> <div> <span>ภาษีมูลค่าเพิ่ม ({vatRate}%)</span> <strong>{money(vat)} บาท</strong> </div> <div className="dashboard-total"> <span>ค่าไฟรวมโดยประมาณ</span> <strong>{money(totalBill)} บาท</strong> </div> </div> </div> </section> <section className="dashboard-panel dashboard-alert-panel"> <div className="dashboard-panel-header"> <div> <h2>การแจ้งเตือน</h2> <p>รายการที่ควรตรวจสอบเกี่ยวกับอุปกรณ์ไฟฟ้า</p> </div> <AlertTriangle size={22} /> </div> {alerts.length === 0 ? ( <div className="dashboard-empty"> <span>✓</span> ไม่พบรายการแจ้งเตือน </div> ) : ( <div className="dashboard-alert-list"> {alerts.map((alert, index) => ( <div className={`dashboard-alert dashboard-alert-${alert.type}`} key={`${alert.type}-${index}`} > <div className="dashboard-alert-icon"> {alert.type === "service" ? ( <CalendarClock size={20} /> ) : ( <AlertTriangle size={20} /> )} </div> <div> <strong>{alert.title}</strong> <p>{alert.text}</p> </div> </div> ))} </div> )} </section> <section className="dashboard-panel"> <div className="dashboard-panel-header"> <div> <h2>แนวโน้มการใช้ไฟฟ้า</h2> <p>พื้นที่สำหรับแสดงกราฟการใช้ไฟฟ้ารายเดือน</p> </div> <TrendingUp size={22} /> </div> <div className="dashboard-chart-placeholder"> <Zap size={32} /> <strong>กราฟการใช้ไฟฟ้ารายเดือน</strong> <span> ระบบสามารถนำข้อมูลการใช้ไฟฟ้าในแต่ละเดือนไปแสดงเป็นกราฟได้ </span> </div> </section> </div> ); } export default DashboardPage;